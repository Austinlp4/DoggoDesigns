/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'localhost',
            'doggodesigns-production.up.railway.app'
        ]
    }
};

export default nextConfig;
